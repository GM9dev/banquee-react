export type SiteBankingData = {
  id: number;
  mainTitle: string;
  description: string;
  listBenefits: Array<string>;
  mainButtonText: string;
  secondButtonText: string;
};

export default SiteBankingData;
